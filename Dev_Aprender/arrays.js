//arrays é um conjunto de dados que pode ser acessado por indice, o indice nada mais nada menos que a posição exata de onde se essa informação

let familia = [true, 50, 'anna', 17];
console.log(familia.length); //length representa um inteiro, dá pra usar pra truncar um array a qualquer momento. Quando vc extende um array mudando sua propriedade length, o numero de elementos atuais não é incrementado;
console.log(familia[0]); //tbm faz parte do objetos então se acessar aqui
